#include<iostream>
using namespace std;

int FMS(int arr[], int low, int high){
    if(high == low){
        return (low, high, arr[low]);
    }
    else{
        int mid = (low + high) / 2;
        
    }
}

int main()
{
    
    return 0;
}